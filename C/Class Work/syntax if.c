#include<stdio.h>
 //Condition : it evalutes to boolean value.
 				// i.e ture(1) or false(0).
 	void main()
 	{
	 
 	
 	int A;
 	
 	printf("Enter value  A : ");
 	scanf("%d",& a);
 	
 	if (a>0)
 	{
 		printf("\nIt is +ve No.");
	}
	else
		printf("\nIt is -ve No");
	}